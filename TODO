




sprawdzic testy dictionary jak odwrocilem podstawową flage na true to czy dobre nazwy testow nadal są

--------------

- pathBaseGenerator
    - najpierw brac savedConfig, potem w alternatywie zwykly config aby latwiej mozna bylo kombinowac algorytmy


--------------

- w debugu w import ma listowac pliki lang-tags.json file:// aby mozna bylo se kliknąć i sprawdzić


--------------

-ma zbierac tez bez konfiguracji

    [08:21:22] Skipping tag "productValidationDialogTranslations = libraryLangTag({
            title: "Inventory validation results",
            description: "Summary of products with validation errors or warnings.",

            invalidCount: "Invalid products: {{count}}",
            warningCount: "Products with just warnings: {{count}}",

            errorsOnly: "Errors only",
            all: "All",

            productDescriptors: "Product descriptors",
            missingDescriptorsCount: "{{count}} products with missing descriptors",
            missingDescriptorsShow: "Show products without descriptors",
            invalidDescriptorsCount: "{{count}} products with invalid descriptors",
            invalidDescriptorsShow: "Show invalid descriptors errors",

            errors: "Errors",
            errorsLabel: "Errors prevent catalog inclusion",
            warnings: "Warnings",
            warningsLabel: "Review recommended",

            uuid: "UUID",
            inventoryIndex: "Inventory index",

            variantDependenciesErrors: "Product variants dependencies errors",
            clickToExpand: "Click to expand",
    })". Tag configuration not defined. (Check lang-tag config at collect.onCollectConfigFix)


--------------


    w init-tag
    wspierac verbalism modules cos tam
    i generowac z "type" w import:

    import {
        type CallableTranslations,
        type LangTagTranslations,
        type LangTagTranslationsConfig,
        type PartialFlexibleTranslations,
        createCallableTranslations,
        normalizeTranslations,
        lookupTranslation
    } from 'lang-tag';
    import React, {